# TrustHire ML — CORE dependencies (classical ML + API + report)
# These run comfortably on a normal laptop CPU. No GPU needed.
# Reproducible install:  pip install -r requirements.txt
#
# Versions are PINNED exactly (resolved on Python 3.14, Windows, via pip freeze)
# so anyone re-running this project gets the same environment and the same
# numbers. All of these ship prebuilt 3.14 wheels — no compiler needed.
#
# NOTE: DistilBERT (the optional upgrade in milestone M7) needs the heavier
#       packages in requirements-distilbert.txt. Install that ONLY when you
#       reach M7, so the core setup stays small and fast.

# --- data handling ---
numpy==2.5.0
pandas==3.0.4
scipy==1.18.0

# --- classical machine learning ---
scikit-learn==1.9.0
joblib==1.5.3

# --- plots for the evaluation report (confusion matrix, ROC curve) ---
matplotlib==3.11.0

# --- inference API (milestone M8) ---
fastapi==0.138.2
uvicorn==0.49.0
pydantic==2.13.4

# --- PDF report generation (milestone M11) ---
reportlab==5.0.0
